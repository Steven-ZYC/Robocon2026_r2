global_navigation_node:
  ros__parameters:
    mission_file: ""                    # set via launch arg; default → routes/mission_1.yaml
    control_rate_hz: 50.0
    arrived_stable_count: 5
    pose_timeout_s: 0.5
